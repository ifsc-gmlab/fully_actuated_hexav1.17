menuconfig MODULES_FLYING_CAR
	bool "Flying-car safety primitives"
	default n
	---help---
		Enable the isolated flying-car safety state machine and its pure support library.
		The runtime flying_car module is added by a later integration task.
